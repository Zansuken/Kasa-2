import { FC } from "react";
import App from "../App";
import Layout from "../components/Layout";

const Home: FC = () => {
  return (
    <Layout>
      <App />
    </Layout>
  );
};

export default Home;
